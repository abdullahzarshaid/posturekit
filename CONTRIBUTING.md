# Contributing

Run `python Code/Tests.py` and `python -m unittest discover -s tests -v` on Python 3.10+.
These synthetic tests do not certify collector behavior on every Windows version.
For rules/importers, supply synthetic input, expected output, negative cases and primary references.
Preserve observation/candidate/verified-impact distinctions. Never submit private configuration,
credentials or client evidence. See SECURITY.md.
